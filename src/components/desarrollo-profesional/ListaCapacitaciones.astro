---
// src/components/desarrollo-profesional/ListaCapacitaciones.astro

const capacitaciones = [
  {
    id: 1,
    titulo: "Desarrollo de Liderazgo",
    descripcion: "Curso intensivo de habilidades de liderazgo",
    instructor: "Ana Martínez",
    fechaInicio: "2024-02-15",
    fechaFin: "2024-02-20",
    estado: "programada",
    participantes: 12,
    modalidad: "presencial"
  },
  // ... más capacitaciones
];
---

<div class="bg-white shadow-md rounded-lg overflow-hidden">
  <div class="p-4 border-b">
    <div class="flex justify-between items-center">
      <h2 class="text-xl font-semibold text-gray-800">Capacitaciones</h2>
      <button id="btnNuevaCapacitacion" class="px-4 py-2 bg-green-500 text-white rounded-lg hover:bg-green-600">
        Nueva Capacitación
      </button>
    </div>
  </div>

  <div class="p-4">
    <div class="grid gap-4">
      {capacitaciones.map(cap => (
        <div class="border rounded-lg p-4 hover:shadow-md transition-shadow">
          <div class="flex justify-between items-start">
            <div>
              <h3 class="font-medium text-gray-900">{cap.titulo}</h3>
              <p class="text-sm text-gray-500 mt-1">{cap.descripcion}</p>
              <div class="mt-2 flex items-center gap-4">
                <span class="text-sm text-gray-600">👤 {cap.instructor}</span>
                <span class="text-sm text-gray-600">📅 {cap.fechaInicio} - {cap.fechaFin}</span>
                <span class="text-sm text-gray-600">👥 {cap.participantes} participantes</span>
              </div>
            </div>
            <div class="flex items-center gap-2">
              <span class={`px-2 py-1 text-xs font-semibold rounded-full 
                ${cap.estado === 'programada' ? 'bg-blue-100 text-blue-800' : 
                cap.estado === 'en_curso' ? 'bg-green-100 text-green-800' : 
                'bg-gray-100 text-gray-800'}`}>
                {cap.estado}
              </span>
              <button class="text-blue-600 hover:text-blue-800">Editar</button>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</div>